# ============================================
# 环境变量配置示例文件
# ============================================
# 复制此文件为 .env.local 来使用本地配置
# 或者根据环境创建对应的配置文件：
#   - .env.development (开发环境)
#   - .env.production (生产环境)
#   - .env.local (本地覆盖，优先级最高，会被 git 忽略)

# ============================================
# API 配置
# ============================================

# API 基础地址（可选）
# 如果设置此值，将优先使用此值，忽略 VITE_ENV 配置
# 示例: VITE_BASE_URL=https://api.example.com
# VITE_BASE_URL=

# 环境类型（可选）
# 如果不设置 VITE_BASE_URL，将根据此值从 urlMap 中选择对应的 baseURL
# 可选值: debug | develop | trial | release
# 默认值: release (生产环境)
# VITE_ENV=release

# ============================================
# 环境说明
# ============================================
# debug    -> http://so.proxy.beanflow.top:82
# develop  -> https://api.beanflow.top:8080
# trial    -> https://api.beanflow.top:8080
# release  -> https://api.beanflow.top (默认)
