---
description: "UI/UX设计规范 - 液态玻璃主题与Apple/微信设计理念"
globs: "src/**/*.vue,src/**/*.scss,src/styles/**"
---

# UI/UX 设计规范

## 设计理念

本项目采用**液态玻璃（Glassmorphism）**视觉主题，结合 **Apple** 和 **微信** 的设计理念，打造现代、优雅、实用的用户界面。

**核心设计原则**：
- ✨ **简洁优雅**：去除不必要的装饰，突出核心内容
- 🎨 **层次分明**：通过透明度和模糊营造空间感和深度
- 💡 **直观易用**：符合用户心理模型，降低学习成本
- 🎯 **以用户为中心**：功能实用，体验流畅
- 🔄 **一致性**：统一的设计语言贯穿整个应用

---

## 一、液态玻璃主题规范

### 1.1 视觉特征

液态玻璃（Glassmorphism）是一种模拟真实玻璃材质的设计风格，具有以下特征：

- **半透明背景**：使用 `rgba()` 或 `hsla()` 实现半透明效果
- **背景模糊**：通过 `backdrop-filter: blur()` 实现毛玻璃效果
- **高光边缘**：使用细边框和高光增强立体感
- **柔和阴影**：多层次阴影营造景深和层次
- **渐变叠加**：适度的渐变增强视觉美感

### 1.2 CSS 实现规范

#### 基础玻璃效果类

```scss
// src/styles/06-components/_glass.scss

// 基础玻璃材质
.glass {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.2);
  box-shadow: 
    0 8px 32px 0 rgba(0, 0, 0, 0.1),
    inset 0 1px 0 0 rgba(255, 255, 255, 0.2);
}

// 暗色主题玻璃材质
.glass-dark {
  background: rgba(0, 0, 0, 0.2);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 
    0 8px 32px 0 rgba(0, 0, 0, 0.3),
    inset 0 1px 0 0 rgba(255, 255, 255, 0.1);
}

// 高强度玻璃（卡片、面板）
.glass-card {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(30px) saturate(180%);
  -webkit-backdrop-filter: blur(30px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.25);
  box-shadow: 
    0 12px 40px 0 rgba(0, 0, 0, 0.15),
    inset 0 1px 0 0 rgba(255, 255, 255, 0.3),
    inset 0 -1px 0 0 rgba(0, 0, 0, 0.05);
}

// 低强度玻璃（悬浮提示、菜单）
.glass-soft {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px) saturate(150%);
  -webkit-backdrop-filter: blur(10px) saturate(150%);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 4px 16px 0 rgba(0, 0, 0, 0.1);
}
```

#### Tailwind 扩展配置

```scss
// src/styles/tailwind.config.js 或 CSS变量

// 在 tailwind.css 中定义液态玻璃相关的工具类
@layer utilities {
  .glass {
    @apply bg-white/10 backdrop-blur-[20px] backdrop-saturate-[180%];
    @apply border border-white/20;
    @apply shadow-[0_8px_32px_0_rgba(0,0,0,0.1),inset_0_1px_0_0_rgba(255,255,255,0.2)];
  }
}
```

### 1.3 透明度层级规范

| 层级 | 用途 | 透明度 | 模糊值 |
|------|------|--------|--------|
| **高强度** | 主要内容卡片、模态框 | 15-25% | 30px |
| **标准** | 普通容器、导航栏 | 10-15% | 20px |
| **低强度** | 悬浮提示、下拉菜单 | 5-10% | 10px |
| **极低** | 背景装饰层 | 2-5% | 5px |

### 1.4 边框规范

- **边框宽度**：通常为 1px，高亮元素可为 1.5px
- **边框颜色**：`rgba(255, 255, 255, 0.1-0.3)`，根据明暗度调整
- **内高光**：使用 `inset` 阴影模拟内高光效果

---

## 二、设计原则（参考 Apple & 微信）

### 2.1 Apple 设计原则

#### 简洁性（Simplicity）
- 去除冗余元素，突出核心功能
- 使用空白空间创造呼吸感
- 避免过度装饰

#### 一致性（Consistency）
- 统一的视觉语言和交互模式
- 相同功能使用相同的视觉表现
- 保持品牌识别度

#### 直接操作（Direct Manipulation）
- 操作结果即时反馈
- 手势操作符合直觉
- 减少中间步骤

#### 反馈（Feedback）
- 所有操作都有明确反馈
- 使用动画传达状态变化
- 错误提示清晰友好

### 2.2 微信设计原则

#### 用完即走
- 减少不必要的功能入口
- 聚焦核心任务
- 流程简洁高效

#### 用户友好
- 降低操作门槛
- 文字说明清晰易懂
- 减少用户思考成本

#### 功能优先
- 实用性大于装饰性
- 信息层级清晰
- 内容为王

---

## 三、颜色系统

### 3.1 主色调

```scss
// src/styles/01-settings/_colors.scss

// 主色（可根据品牌调整）
$color-primary: #007AFF;      // Apple Blue
$color-primary-hover: #0051D5;
$color-primary-active: #0040B8;

// 辅助色
$color-success: #34C759;      // Green
$color-warning: #FF9500;      // Orange
$color-danger: #FF3B30;       // Red
$color-info: #5AC8FA;         // Light Blue

// 中性色
$color-text-primary: #000000;
$color-text-secondary: #6B7280;
$color-text-tertiary: #9CA3AF;
$color-text-disabled: #D1D5DB;

// 背景色（用于玻璃效果叠加）
$color-bg-primary: rgba(255, 255, 255, 0.9);      // 主背景
$color-bg-secondary: rgba(255, 255, 255, 0.7);    // 次要背景
$color-bg-glass: rgba(255, 255, 255, 0.1);        // 玻璃背景

// 暗色模式
$color-text-primary-dark: #FFFFFF;
$color-text-secondary-dark: #A1A1AA;
$color-bg-glass-dark: rgba(0, 0, 0, 0.2);
```

### 3.2 颜色使用规范

- **主色**：用于主要按钮、链接、选中状态
- **辅助色**：用于状态提示（成功/警告/错误）
- **中性色**：用于文字、边框、分隔线
- **玻璃色**：用于容器背景，配合 `backdrop-filter` 使用

### 3.3 对比度要求

- **正文文字**：与背景对比度 ≥ 4.5:1（WCAG AA）
- **大号文字**：与背景对比度 ≥ 3:1（WCAG AA）
- **交互元素**：清晰可见，有明确的视觉反馈

---

## 四、排版系统

### 4.1 字体规范

```scss
// src/styles/04-elements/_typography.scss

// 字体族（优先使用系统字体）
$font-family-base: -apple-system, BlinkMacSystemFont, "Segoe UI", 
                   "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", 
                   "Helvetica Neue", Helvetica, Arial, sans-serif;

// 字体大小（参考 Apple 设计）
$font-size-xs: 12px;      // 辅助信息
$font-size-sm: 14px;      // 次要信息
$font-size-base: 16px;    // 正文（基准）
$font-size-lg: 18px;      // 强调文本
$font-size-xl: 20px;      // 小标题
$font-size-2xl: 24px;     // 标题
$font-size-3xl: 30px;     // 大标题
$font-size-4xl: 36px;     // 超大标题

// 行高
$line-height-tight: 1.2;
$line-height-normal: 1.5;
$line-height-relaxed: 1.8;

// 字重
$font-weight-light: 300;
$font-weight-normal: 400;
$font-weight-medium: 500;
$font-weight-semibold: 600;
$font-weight-bold: 700;
```

### 4.2 排版层级

| 类型 | 字体大小 | 字重 | 行高 | 用途 |
|------|----------|------|------|------|
| **超大标题** | 36px | 700 | 1.2 | 页面主标题 |
| **大标题** | 30px | 700 | 1.2 | 区块标题 |
| **标题** | 24px | 600 | 1.3 | 卡片标题 |
| **小标题** | 20px | 600 | 1.4 | 分组标题 |
| **强调文本** | 18px | 500 | 1.5 | 重要信息 |
| **正文** | 16px | 400 | 1.6 | 主要内容 |
| **次要文本** | 14px | 400 | 1.5 | 辅助信息 |
| **辅助信息** | 12px | 400 | 1.4 | 说明文字 |

### 4.3 文本颜色层级

- **一级文本**：`color-text-primary` - 主要内容
- **二级文本**：`color-text-secondary` - 次要内容
- **三级文本**：`color-text-tertiary` - 辅助信息
- **禁用文本**：`color-text-disabled` - 不可用状态

---

## 五、间距系统

### 5.1 间距规范（参考 8px 网格系统）

```scss
// src/styles/01-settings/_spacing.scss

// 间距基数：4px
$spacing-base: 4px;

// 间距值（4px 倍数）
$spacing-1: 4px;      // 极小间距
$spacing-2: 8px;      // 小间距
$spacing-3: 12px;     // 较小间距
$spacing-4: 16px;     // 标准间距
$spacing-5: 20px;     // 中等间距
$spacing-6: 24px;     // 较大间距
$spacing-8: 32px;     // 大间距
$spacing-10: 40px;    // 超大间距
$spacing-12: 48px;    // 特大间距
$spacing-16: 64px;    // 巨大间距
```

### 5.2 间距使用规范

- **组件内间距**：使用 4px、8px、12px
- **组件间距**：使用 16px、24px
- **区块间距**：使用 32px、40px、48px
- **页面边距**：使用 16px-24px（移动端），24px-32px（桌面端）

### 5.3 圆角规范

```scss
// 圆角值
$radius-sm: 4px;      // 小元素（标签、徽章）
$radius-base: 8px;    // 标准（按钮、输入框）
$radius-md: 12px;     // 中等（卡片）
$radius-lg: 16px;     // 大（模态框、面板）
$radius-xl: 24px;     // 超大（特殊容器）
$radius-full: 9999px; // 完全圆形
```

---

## 六、组件设计规范

### 6.1 按钮规范

#### 主要按钮（Primary）
```scss
.btn-primary {
  @apply px-6 py-3 rounded-lg;
  background: linear-gradient(135deg, #007AFF 0%, #0051D5 100%);
  color: #FFFFFF;
  font-weight: 500;
  box-shadow: 0 4px 12px rgba(0, 122, 255, 0.3);
  transition: all 0.2s ease;
  
  &:hover {
    transform: translateY(-1px);
    box-shadow: 0 6px 16px rgba(0, 122, 255, 0.4);
  }
  
  &:active {
    transform: translateY(0);
  }
}
```

#### 次要按钮（Secondary - 玻璃风格）
```scss
.btn-secondary {
  @apply px-6 py-3 rounded-lg glass;
  color: #007AFF;
  font-weight: 500;
  transition: all 0.2s ease;
  
  &:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.3);
  }
}
```

### 6.2 卡片规范

```scss
.card {
  @apply glass-card rounded-xl p-6;
  transition: all 0.3s ease;
  
  &:hover {
    transform: translateY(-2px);
    box-shadow: 
      0 16px 48px 0 rgba(0, 0, 0, 0.2),
      inset 0 1px 0 0 rgba(255, 255, 255, 0.3);
  }
}
```

### 6.3 输入框规范

```scss
.input {
  @apply px-4 py-3 rounded-lg glass-soft;
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: $color-text-primary;
  transition: all 0.2s ease;
  
  &:focus {
    outline: none;
    border-color: #007AFF;
    box-shadow: 0 0 0 3px rgba(0, 122, 255, 0.1);
    background: rgba(255, 255, 255, 0.15);
  }
  
  &::placeholder {
    color: $color-text-tertiary;
  }
}
```

### 6.4 导航栏规范

```scss
.navbar {
  @apply glass;
  padding: 12px 24px;
  backdrop-filter: blur(20px) saturate(180%);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
```

---

## 七、交互原则

### 7.1 动画与过渡

#### 动画时长规范

```scss
$duration-fast: 150ms;      // 快速反馈（hover、点击）
$duration-base: 200ms;      // 标准过渡
$duration-slow: 300ms;      // 慢速过渡（页面切换）
$duration-slower: 500ms;    // 更慢过渡（复杂动画）
```

#### 缓动函数

```scss
// 参考 Apple 动画曲线
$ease-out: cubic-bezier(0.33, 1, 0.68, 1);      // 自然退出
$ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);   // 标准缓动
$ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55); // 弹性动画
```

### 7.2 交互反馈

#### 悬停状态（Hover）
- 轻微上移（`transform: translateY(-2px)`）
- 增强阴影或高光
- 颜色/透明度变化

#### 点击状态（Active）
- 轻微下压（`transform: translateY(0)` 或 `scale(0.98)`）
- 即时视觉反馈

#### 加载状态
- 使用骨架屏（Skeleton Screen）
- 平滑的加载动画
- 清晰的进度指示

### 7.3 手势操作

- **点击**：主要操作方式
- **长按**：次要操作（上下文菜单）
- **滑动**：列表滚动、页面切换
- **拖拽**：元素排序、调整大小

---

## 八、响应式设计

### 8.1 断点规范

```scss
// 断点（参考 Tailwind）
$breakpoint-sm: 640px;   // 小屏（手机横屏）
$breakpoint-md: 768px;   // 中屏（平板竖屏）
$breakpoint-lg: 1024px;  // 大屏（平板横屏、小笔记本）
$breakpoint-xl: 1280px;  // 超大屏（桌面）
$breakpoint-2xl: 1536px; // 2K屏
```

### 8.2 适配策略

- **移动优先**：从小屏幕开始设计
- **弹性布局**：使用 Flexbox 和 Grid
- **相对单位**：优先使用 `rem`、`em`、`%`、`vw/vh`
- **图片适配**：响应式图片，支持不同分辨率

---

## 九、可访问性（A11y）

### 9.1 基本要求

- **键盘导航**：所有交互元素支持键盘操作
- **焦点指示**：清晰的焦点样式（`:focus-visible`）
- **颜色对比度**：符合 WCAG AA 标准
- **语义化HTML**：正确使用 HTML5 语义标签

### 9.2 无障碍属性

```vue
<!-- 示例：为交互元素添加无障碍属性 -->
<button
  aria-label="关闭对话框"
  aria-describedby="dialog-description"
>
  <span aria-hidden="true">×</span>
</button>
```

---

## 十、暗色模式支持

### 10.1 实现方式

```scss
// 使用 CSS 变量和媒体查询
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-glass: rgba(0, 0, 0, 0.2);
    --color-text-primary: #FFFFFF;
    --color-text-secondary: #A1A1AA;
  }
}

// 或使用类切换
.theme-dark {
  --color-bg-glass: rgba(0, 0, 0, 0.2);
  // ...
}
```

### 10.2 暗色模式玻璃效果调整

- 降低背景透明度
- 增强边框可见度
- 调整文字颜色对比度
- 优化阴影效果

---

## 十一、性能优化

### 11.1 玻璃效果优化

- **适度使用**：避免过度使用 `backdrop-filter`（性能开销大）
- **降级方案**：低端设备使用纯色背景
- **GPU 加速**：使用 `transform` 和 `opacity` 触发硬件加速
- **减少层级**：避免多层玻璃效果叠加

### 11.2 动画优化

```scss
// 使用 will-change 提示浏览器优化
.animated-element {
  will-change: transform, opacity;
  
  &.is-animating {
    // 动画进行中
  }
  
  // 动画结束后移除
  &:not(.is-animating) {
    will-change: auto;
  }
}
```

---

## 十二、实施检查清单

在设计新页面或组件时，请检查以下事项：

### 视觉设计
- [ ] 使用了液态玻璃主题效果
- [ ] 颜色符合颜色系统规范
- [ ] 间距符合 8px 网格系统
- [ ] 圆角统一规范
- [ ] 阴影效果合理

### 交互设计
- [ ] 所有交互元素有明确的反馈
- [ ] 动画时长和缓动函数符合规范
- [ ] 支持键盘导航
- [ ] 焦点状态清晰可见

### 响应式
- [ ] 在不同屏幕尺寸下测试
- [ ] 移动端体验良好
- [ ] 触摸目标大小合适（≥ 44×44px）

### 可访问性
- [ ] 文字对比度符合标准
- [ ] 语义化 HTML
- [ ] 无障碍属性完善
- [ ] 支持暗色模式

### 性能
- [ ] 动画流畅（60fps）
- [ ] 玻璃效果不影响性能
- [ ] 图片优化加载

---

## 十三、参考资源

- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [Material Design 3](https://m3.material.io/)
- [Web Content Accessibility Guidelines (WCAG)](https://www.w3.org/WAI/WCAG21/quickref/)
- [Glassmorphism CSS Generator](https://glassmorphism.com/)

---

## 注意事项

1. **平衡美观与性能**：液态玻璃效果虽美观，但需注意性能影响
2. **保持一致性**：同一功能在不同页面使用相同的视觉表现
3. **以用户为中心**：设计服务于功能，避免过度设计
4. **渐进增强**：低端设备提供降级方案
5. **持续优化**：根据用户反馈和使用数据持续优化设计
